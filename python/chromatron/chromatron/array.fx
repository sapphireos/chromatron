
a = Array(4, publish=True)
b = Number()
c = Array(3)

# MyRecord = Record(meow=Number(), 
                  # stuff=Array(2))

# MyRecord = Record(meow=Number())

def init():
    pass

def loop():
    # pass
    
    # a += 2


    # pixels.val = 1
    # a[1] = 2
    # b = a[1]
    # c[2] = a[b]
    b = len(a)
    # b = max(a)
    # pixels[1].hue = 2

    # b[0][1] = 3
    