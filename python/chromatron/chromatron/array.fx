
a = Array(4)
b = Number()
# b = Array(3, 3)

# MyRecord = Record(meow=Number(), 
                  # stuff=Array(2))

# MyRecord = Record(meow=Number())

def init():
    pass

def loop():
    # pass
    
    a += 2
    # pixels.val = 1
    # a[1] = 2
    # b = a[1]
    # b = len(a)
    # b = max(a)
    # pixels[1].hue = 2

    # b[0][1] = 3
    